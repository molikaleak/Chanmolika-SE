# Vite environment variables
# These variables are exposed to your client-side code via import.meta.env
# Prefix with VITE_ to expose them to the client

# API Configuration
VITE_API_BASE_URL=http://localhost:4000

# Vercel deployment will require these to be set in the Vercel project settings
# Add any other environment variables needed for your application here